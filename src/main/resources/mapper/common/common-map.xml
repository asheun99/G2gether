<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yedam.app.common.mapper.CommonMapper">
<select id="selectAll" resultType="CommonVO">
	SELECT last_name
	FROM   employees
	ORDER BY 1
</select>
</mapper>